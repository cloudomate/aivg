pytest>=8
pytest-asyncio>=0.23
ruff>=0.4
black>=24
# Runtime extras for the production transport (not needed for the test suite,
# which runs against the in-memory fake transport + fake Hermes bridge):
# aiortc>=1.6
# aiohttp>=3.9
# av>=11
